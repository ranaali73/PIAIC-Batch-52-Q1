// Write a program that uses a for loop to print the first 25 integers.
function printIntegers(len: number) {
    for (let i = 1; i <= len; i++) {
        console.log(i);
    }
}

// Function Call
printIntegers(25);
